# For the Telegram Bot framework (modern async version)
python-telegram-bot[ext]>=20.0

# For connecting to the MongoDB database
pymongo[srv]

# For loading environment variables locally (optional but good practice)
python-dotenv
